<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>SA Backoffice</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <header class="header">
        <%= render "header.html", conn: @conn, current_user: assigns[:current_user] %>
      </header>

      <%= if get_flash(@conn, :info) != nil do %>
        <article class="message is-info" role="alert">
          <div class="message-body">
            <%= get_flash(@conn, :info) %>
          </div>
        </article>
      <% end %>

      <%= if get_flash(@conn, :error) != nil do %>
        <article class="message is-error" role="alert">
          <div class="message-body">
            <%= get_flash(@conn, :error) %>
          </div>
        </article>
      <% end %>

      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>
    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
