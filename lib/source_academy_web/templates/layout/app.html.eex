<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="theme-color" content="#000000">
    <title>Source Academy</title>
    <%= if Mix.env == :dev do %>
      <script src="http://localhost:4001/static/js/common.js"></script>
    <% else %>
      <link rel="stylesheet" href="<%= static_path(@conn, "/css/common.css") %>">
      <script src="<%= static_path(@conn, "/js/common.js") %>"></script>
    <% end %>
  </head>
  <body>
    <%= render @view_module, @view_template, assigns %>
    <%= if Mix.env == :dev do %>
      <script src="http://localhost:4001/static/js/academy.js"></script>
    <% else %>
      <script src="<%= static_path(@conn, "/js/academy.js") %>"></script>
    <% end %>
  </body>
</html>
